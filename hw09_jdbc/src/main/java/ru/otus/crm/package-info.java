/**
 * Содержит в себе пул подключений, бизнес сущности, и CRUD операции
 * <p>
 * CRUD операции сделаны за счёт универсального класса-репозитория, позволяющего работать с разными бизнес сущностями
 * и обёртки над этим репозиторием в виде сервисного слоя для конкретной бизнес сущности.
 * </p>
 *
 */
package ru.otus.crm;